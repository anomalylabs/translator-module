{% macro keys(keys) %}
    {% import _self as self %}

    {% for key, value in keys %}
        {% if value is iterable %}
            '{{ key }}' => [{{ self.keys(value) }}],
        {% else %}
            '{{ key }}' => '{{ value|raw }}',
        {% endif %}
    {% endfor %}
{% endmacro %}
